You are a senior full-stack product engineer. Upgrade and deploy my existing Next.js app called ScopeBridge AI / DeliveryGuard AI into a polished AI Chief of Staff product.

Important security rule:
Do not hardcode or print any real secrets. Use environment variable placeholders only. Assume previous credentials were leaked and must be rotated. Add .env.local to .gitignore. Create .env.example with placeholder names only.

Current app:
The current UI is a dark three-column risk dashboard with Source Inbox, Delivery Risks, and Actions. It works but feels too plain. I uploaded a zip with reference screens from a more polished product called ContextPilot. Use that visual direction: dark premium interface, vibrant lavender and mint accents, inclusive accessible color contrast, left sidebar, large confident page titles, command/search bar, glassy cards, terminal streams, diff panels, action center, source connectors, and clear CTAs.

Goal:
Build the best possible hackathon-ready version of ScopeBridge AI: an AI delivery-risk auditor and action orchestrator that connects Gmail, Slack, GitHub, manual text, screenshots/images, code snippets, and uploaded files, builds a 128k-token context pack, detects contradictions, scope drift, blockers, missing tickets, delivery risk, and then lets the user create or send the right action through one approved button.

Tech stack:
- Next.js App Router with TypeScript
- React
- Tailwind CSS
- shadcn/ui or equivalent local UI components
- Prisma ORM
- PostgreSQL
- Auth.js / NextAuth v5 style setup
- HydraDB for long-context memory and retrieval
- Pipeshift AI endpoint using MODEL_NAME=moonshotai/Kimi-K2.6
- Gmail API
- Slack Web API
- GitHub REST API
- SSE streaming for audit progress

Core implementation requirements:

1. Redesign UI
Replace the current plain three-column layout with a polished dashboard inspired by the uploaded ContextPilot zip.

Create these pages:
- /dashboard/pulse
  - System Pulse page
  - Health score
  - Connector sync cards for Gmail, Slack, GitHub, HydraDB
  - Recent audit stream
  - Big CTA: Run Audit
  - Big CTA: Inject Context
- /dashboard/insights
  - Delivery Risk / Insight Center
  - Cards for contradictions, missing ticket, scope drift, blockers, stale promises, PR conflicts
  - Evidence panels showing source snippets from email, Slack, GitHub, uploaded screenshot, or manual text
  - Action Plan section
  - Ask ScopeBridge input
- /dashboard/sources
  - Connector buttons/cards:
    - Connect Gmail
    - Connect Slack
    - Connect GitHub
    - Connect HydraDB
    - Manual Upload
    - Screenshot/Image Upload
    - Code Paste
  - Status badges: connected, disconnected, syncing, failed
  - Source filters: Gmail query, Slack channel, GitHub repo, date range, labels/domains
- /dashboard/memory
  - Memory Audit & Diff page
  - Compare previous context vs latest context
  - Show red/green diff blocks
  - Show contradictions and new commitments
  - Accept Merges button
- /dashboard/actions
  - Action Center
  - Draft email
  - Draft Slack message
  - Draft GitHub issue
  - Draft PR
  - One approved execution button per action
- /dashboard/settings
  - User profile
  - Workspace settings
  - Token/context budget settings
  - Connector scopes
  - Danger zone for disconnecting integrations

Design style:
- Dark base: near-black background, slightly lighter panels
- Accent colors: lavender, mint green, cyan, soft pink/red for risk
- Use large page titles, roomy spacing, rounded cards, thin borders, subtle shadows, command palette feel
- Use accessible contrast and keyboard-friendly controls
- Mobile responsive, but optimize for desktop demo
- Include beautiful empty states and loading states
- Use skeleton loaders and streaming audit logs
- Keep product name as ScopeBridge AI unless existing branding uses DeliveryGuard AI, then use ScopeBridge AI in nav and DeliveryGuard as one demo workspace

2. Auth.js and PostgreSQL user persistence
Implement Auth.js with PostgreSQL persistence through Prisma.

Add:
- auth.ts
- app/api/auth/[...nextauth]/route.ts
- middleware.ts protecting /dashboard
- lib/prisma.ts
- prisma/schema.prisma

Use PrismaAdapter. Store all user and workspace data in Postgres.

Minimum Prisma models:
- User
- Account
- Session
- VerificationToken
- Workspace
- WorkspaceMember
- ConnectedAccount
- Source
- SourceItem
- ContextDocument
- AuditRun
- Risk
- RiskEvidence
- ActionDraft
- ActionExecution
- MemorySnapshot
- UploadedAsset
- ConnectorSyncJob

Important:
- OAuth/access/refresh tokens in ConnectedAccount must be encrypted before storing in Postgres.
- Use APP_ENCRYPTION_KEY with AES-GCM or a safe equivalent.
- Never store plain tokens.
- Add audit logs for all external send/push actions.
- User data must be scoped by userId and workspaceId.

3. Environment variables
Create .env.example with placeholders:

DATABASE_URL=
AUTH_SECRET=
APP_ENCRYPTION_KEY=

PIPESHIFT_API_KEY=
PIPESHIFT_BASE_URL=https://api.pipeshift.com/api/v0
MODEL_NAME=moonshotai/Kimi-K2.6
MAX_CONTEXT_TOKENS=128000

HYDRADB_API_KEY=
HYDRADB_BASE_URL=https://api.hydradb.com
HYDRADB_PROJECT_ID=

GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=http://localhost:3000/api/integrations/gmail/callback

SLACK_CLIENT_ID=
SLACK_CLIENT_SECRET=
SLACK_SIGNING_SECRET=
SLACK_BOT_TOKEN=

GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=
GITHUB_WEBHOOK_SECRET=
GITHUB_TOKEN=

NEXT_PUBLIC_APP_URL=http://localhost:3000
REALTIME_MODE=sse
DEMO_MODE=true

4. Connector flows
Build connector buttons and OAuth/server routes.

Gmail:
- Connect Gmail through OAuth
- Scopes should support reading specific emails and sending emails
- Let user fetch messages with:
  - q search query
  - labels
  - allowed domains
  - max results
  - date range
- Use Gmail users.messages.list with q and labelIds
- Fetch message details after listing IDs
- Normalize email body, sender, subject, date, threadId
- Store in SourceItem and optionally ingest to HydraDB
- Send email using Gmail messages.send with MIME base64url raw message
- Always show a draft preview before sending
- Button: Approve & Send Email

Slack:
- Connect Slack
- Show workspace/channel status
- Let user pick channel or user ID
- Let user send a specific Slack message using bot token
- Use chat.postMessage
- Store sent message metadata in ActionExecution
- Button: Approve & Send Slack Message

GitHub:
- Connect GitHub
- Fetch user repos or fetch a specific repo by owner/name
- Pull relevant repo context:
  - README
  - open issues
  - open PRs
  - recent commits
  - selected files
- Store repo context as SourceItem
- Add one-button action to create:
  - GitHub issue draft
  - branch and file change if requested
  - pull request
- For PR creation, create branch/commit first if needed, then create pull request with title, head, base, and body
- Button: Approve & Open PR
- Do not directly merge PRs

HydraDB:
Implement lib/hydradb.ts with typed wrappers for:
- POST /tenants/create
- GET /tenants/monitor
- GET /tenants/infra/status
- POST /ingestion/upload_knowledge
- POST /memories/add_memory
- POST /recall/full_recall
- POST /recall/boolean_recall
- POST /list/data
- POST /fetch/content
- GET /relations or the documented graph relations endpoint

Use @hydra_db/node if it works cleanly, otherwise use fetch wrappers.

Use tenant_id = workspace slug or HYDRADB_PROJECT_ID.
Use sub_tenant_id = user id or workspace id depending on scope.

5. Context ingestion
Create a unified context intake flow:
Inputs:
- Connected Gmail results
- Connected Slack messages
- GitHub repo context
- Manual text prompt
- Uploaded files: txt, md, csv, json, pdf if parser exists
- Screenshot/image upload
- Code snippets pasted by user

For images:
- Store image metadata in UploadedAsset
- Send image to model if Pipeshift supports multimodal for this model/provider
- If not supported, extract user-provided notes and store image reference only
- Do not fake OCR unless implemented

Context composer:
- Build a 128k-token context window
- Deduplicate repeated snippets
- Rank by recency, relevance, source trust, and direct user selection
- Keep evidence anchors with source IDs
- Produce a ContextPack object:
  - user_prompt
  - selected_sources
  - source_summaries
  - raw_evidence_snippets
  - code_context
  - image_context
  - hydradb_recall
  - action_capabilities
  - token_budget_report

Create a visible token budget meter.

6. AI audit and reasoning UX
Implement Run Audit:
- Pull selected source context
- Query HydraDB full recall in mode="thinking" for deep multi-source context
- Query boolean recall for exact ticket IDs, issue IDs, names, dates, PR numbers
- Compose prompt for Kimi K2.6 with max app context of 128000 tokens
- Stream progress via SSE:
  - syncing sources
  - retrieving memory
  - building context
  - analyzing contradictions
  - drafting actions
  - complete

Important:
Do not expose hidden chain-of-thought. Instead show an “Expanded Analysis” panel with:
- Evidence used
- Assumptions
- Risk classification
- Why it matters
- Recommended next action
- Confidence score
- Missing information

AI output JSON schema:
{
  "summary": string,
  "risks": [
    {
      "title": string,
      "severity": "critical" | "high" | "medium" | "low",
      "category": "missing_ticket" | "contradiction" | "scope_drift" | "blocked_backend" | "stale_promise" | "repo_risk" | "other",
      "confidence": number,
      "client_promise": string,
      "engineering_reality": string,
      "evidence": [
        {
          "sourceType": "gmail" | "slack" | "github" | "manual" | "image" | "file" | "hydradb",
          "sourceId": string,
          "quote": string,
          "timestamp": string | null
        }
      ],
      "recommended_action": string
    }
  ],
  "action_drafts": [
    {
      "type": "email" | "slack" | "github_issue" | "github_pr",
      "title": string,
      "body": string,
      "target": string,
      "requiresConfirmation": true
    }
  ],
  "memory_updates": [
    {
      "kind": "commitment" | "preference" | "project_fact" | "risk_pattern",
      "text": string
    }
  ]
}

Persist risks and action drafts to Postgres.

7. Action Center
When a risk is selected, show possible actions:
- Client Email Draft
- Slack Escalation
- GitHub Issue Draft
- GitHub PR Draft
- Store Memory
- Accept Memory Merge

Each action must:
- Show source evidence
- Show generated draft
- Let user edit
- Have one clear button:
  - Approve & Send
  - Approve & Post
  - Approve & Create Issue
  - Approve & Open PR
- Log success/failure in ActionExecution
- Store successful patterns back into HydraDB with POST /memories/add_memory

8. API routes
Create route handlers:

Auth:
- app/api/auth/[...nextauth]/route.ts

Connectors:
- app/api/integrations/gmail/connect/route.ts
- app/api/integrations/gmail/callback/route.ts
- app/api/integrations/gmail/search/route.ts
- app/api/integrations/gmail/send/route.ts
- app/api/integrations/slack/connect/route.ts
- app/api/integrations/slack/callback/route.ts
- app/api/integrations/slack/send/route.ts
- app/api/integrations/github/connect/route.ts
- app/api/integrations/github/callback/route.ts
- app/api/integrations/github/repos/route.ts
- app/api/integrations/github/create-issue/route.ts
- app/api/integrations/github/create-pr/route.ts

AI:
- app/api/audit/run/route.ts
- app/api/audit/stream/route.ts
- app/api/context/build/route.ts
- app/api/context/upload/route.ts
- app/api/memory/recall/route.ts
- app/api/memory/list/route.ts
- app/api/memory/accept/route.ts

Settings:
- app/api/settings/connectors/route.ts
- app/api/settings/workspace/route.ts

9. Demo mode
Keep DEMO_MODE=true support.
If real connector tokens are missing:
- Show beautiful simulated connector cards
- Run audit from seeded demo data
- Still persist demo audit runs in Postgres
- Clearly label demo data as demo

Seed demo data:
- A client asks for SSO before pilot
- Engineering says SSO is not scoped
- Backend deployment blocked by API keys
- Mobile demo requested but out of original scope
- GitHub PR introduces auth conflict
- Slack says timeline moved
- Email says client was promised earlier date

10. Deployment
Prepare for Vercel deployment:
- Ensure build passes
- Add prisma generate
- Add migration instructions
- Add README deployment section
- Add npm scripts:
  - dev
  - build
  - start
  - lint
  - prisma:generate
  - prisma:migrate
  - prisma:seed

Deployment checklist:
- Rotate all secrets
- Put secrets in Vercel environment variables
- Run prisma migrate deploy
- Verify Auth.js callback URLs
- Verify Gmail OAuth redirect URL
- Verify Slack OAuth redirect URL
- Verify GitHub OAuth callback URL
- Verify NEXT_PUBLIC_APP_URL
- Run production smoke test

11. Acceptance criteria
The build is done when:
- User can sign in
- User lands on redesigned dashboard
- Connector buttons exist and show connected state
- User can fetch specific Gmail messages by query
- User can draft and send an email after approval
- User can send a Slack bot message after approval
- User can fetch a specific GitHub repo
- User can create a GitHub issue or PR draft and approve it
- User can upload text/image/code context
- Run Audit uses connected context plus HydraDB recall
- Risks are shown with evidence and confidence
- Action drafts are generated
- All user/workspace data persists in Postgres
- No real secrets are committed
- `npm run build` passes

Now inspect the existing codebase first, preserve working parts, then implement this in clean commits. Prioritize a stunning demo-quality UI and reliable end-to-end flows over over-engineered abstractions.